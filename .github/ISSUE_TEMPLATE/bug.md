---
name: Bug
about: Something behaves differently than documented
labels: bug
---

**What you ran** (redact credentials):

```bash
moneylover ...
```

**What happened** vs **what you expected**:

**Backend** — `web` (default) or `mobile`:

**Version** — `moneylover --help` header, or the npm version, plus `node -v`:
