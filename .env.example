# Copy to .env. Used by compose.yaml; the CLI reads the same names.
MONEYLOVER_EMAIL=you@example.com
MONEYLOVER_PASSWORD=

# The bearer token clients must send. Generate one:  openssl rand -hex 32
MCP_TOKEN=
